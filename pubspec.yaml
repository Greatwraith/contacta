name: bikin_crud
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
    # Package utama Flutter, WAJIB ada di setiap project Flutter
    # Berisi semua widget dasar seperti Text, Row, Column, Scaffold, dll
    # 'sdk: flutter' artinya diambil langsung dari Flutter SDK, bukan dari pub.dev

  cupertino_icons: ^1.0.8
    # Package ikon bergaya iOS (Apple/Cupertino)
    # Digunakan saat pakai CupertinoIcons.namaIkon di dalam widget
    # Contoh: CupertinoIcons.home, CupertinoIcons.search
    # Tanda ^ artinya boleh update otomatis selama masih versi 1.x.x

  firebase_core: ^3.15.2
    # Package WAJIB pertama kali sebelum menggunakan layanan Firebase apapun
    # Bertugas menginisialisasi koneksi antara aplikasi Flutter dengan project Firebase
    # Harus dipanggil Firebase.initializeApp() di main.dart sebelum runApp()
    # Tanpa package ini, semua package Firebase lainnya tidak akan bisa berjalan

  cloud_firestore: ^5.4.4
    # Package untuk menggunakan database NoSQL Firestore milik Firebase
    # Digunakan untuk operasi CRUD: Create, Read, Update, Delete data
    # Mendukung realtime listener (StreamBuilder) — data otomatis update tanpa refresh
    # Contoh penggunaan: FirebaseFirestore.instance.collection('users').get()
    # Dipakai di project ini untuk menyimpan data kontak dan kategori

  firebase_auth: ^5.7.0
    # Package untuk sistem autentikasi/login menggunakan Firebase
    # Mendukung login dengan: Email & Password, Google, Facebook, Phone, dll
    # Menyediakan FirebaseAuth.instance.currentUser untuk cek user yang sedang login
    # Contoh: FirebaseAuth.instance.signInWithEmailAndPassword(...)
    # Dipakai di project ini untuk mengambil UID pengguna yang sedang aktif

  shared_preferences: ^2.2.3
    # Package untuk menyimpan data kecil secara LOKAL di perangkat pengguna
    # Cocok untuk menyimpan: status login, pengaturan tema, preferensi pengguna
    # Data tersimpan dalam format key-value (seperti HashMap/Dictionary)
    # Data tetap ada meskipun aplikasi ditutup (persistent storage)
    # Contoh: await prefs.setString('token', 'abc123') / prefs.getString('token')
    # TIDAK cocok untuk data besar atau data yang butuh relasi antar tabel


dev_dependencies:
  flutter_test:
    sdk: flutter
    # Package khusus untuk membuat unit test dan widget test di Flutter
    # Hanya aktif saat proses development/pengujian, TIDAK ikut di build production
    # Berisi fungsi seperti expect(), find(), tester.pump() untuk testing UI

  flutter_lints: ^6.0.0
    # Package berisi aturan penulisan kode (linting rules) yang direkomendasikan Flutter
    # Membantu mendeteksi kode yang buruk, tidak efisien, atau tidak mengikuti standar
    # Hanya aktif saat development, memberikan peringatan/error di editor (VS Code, Android Studio)
    # Contoh peringatan: variabel tidak digunakan, tipe data tidak eksplisit, dll

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/logo-contacta.PNG
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
